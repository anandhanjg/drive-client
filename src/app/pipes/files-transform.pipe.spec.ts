import { FilesTransformPipe } from './files-transform.pipe';

describe('FilesTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new FilesTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
