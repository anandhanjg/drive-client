import { PathSplitterPipe } from './path-splitter.pipe';

describe('PathSplitterPipe', () => {
  it('create an instance', () => {
    const pipe = new PathSplitterPipe();
    expect(pipe).toBeTruthy();
  });
});
